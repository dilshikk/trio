// Auth hook placeholder — Hercules Auth removed from this project
// If auth is needed in the future, re-add @usehercules/auth
export function useAuth() {
  return { user: null, isLoading: false };
}
